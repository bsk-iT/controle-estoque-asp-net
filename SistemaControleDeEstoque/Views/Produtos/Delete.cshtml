@model SistemaControleDeEstoque.Models.Produto

@{
    ViewData["Title"] = "Excluir Produto";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="flex justify-center pt-3">
    <div class="w-full max-w-sm">

        <!-- Cabeçalho centralizado -->
        <div class="mb-8 text-center">
            <p class="text-xs uppercase tracking-widest text-gray-400 dark:text-gray-600 mb-1">inventarii</p>
            <h1 class="text-2xl font-light text-red-600 dark:text-red-500 tracking-tight"
                style="font-family: 'Playfair Display', Georgia, serif;">Excluir Produto</h1>
        </div>

        <!-- Alerta de exclusão -->
        <div class="mb-6 border border-red-200 dark:border-red-900 rounded-lg p-4">
            <p class="text-sm font-medium text-red-700 dark:text-red-400">Tem certeza que deseja excluir este produto?</p>
            <p class="text-sm text-red-600 dark:text-red-500 mt-1">Esta ação não pode ser desfeita.</p>
        </div>

        <!-- Dados do produto em lista vertical -->
        <div class="border border-gray-200 dark:border-gray-800 rounded-lg divide-y divide-gray-100 dark:divide-gray-800 mb-6">

            <!-- Nome -->
            <div class="px-6 py-4">
                <p class="text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider mb-1">
                    @Html.DisplayNameFor(model => model.Nome)
                </p>
                <p class="text-sm font-medium text-gray-900 dark:text-white">@Html.DisplayFor(model => model.Nome)</p>
            </div>

            <!-- Tipo -->
            <div class="px-6 py-4">
                <p class="text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider mb-1">
                    @Html.DisplayNameFor(model => model.Tipo)
                </p>
                <p class="text-sm font-medium text-gray-900 dark:text-white">@Html.DisplayFor(model => model.Tipo)</p>
            </div>

            <!-- Quantidade -->
            <div class="px-6 py-4">
                <p class="text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider mb-1">
                    @Html.DisplayNameFor(model => model.Quantidade)
                </p>
                <p class="text-sm font-medium text-gray-900 dark:text-white">@Html.DisplayFor(model => model.Quantidade) unidade(s)</p>
            </div>

            <!-- Valor -->
            <div class="px-6 py-4">
                <p class="text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider mb-1">
                    @Html.DisplayNameFor(model => model.Valor)
                </p>
                <p class="text-sm font-medium text-gray-900 dark:text-white">
                    @Model.Valor.ToString("C", new System.Globalization.CultureInfo("pt-BR"))
                </p>
            </div>

            <!-- Fornecedor -->
            <div class="px-6 py-4">
                <p class="text-xs font-medium text-gray-500 dark:text-gray-400 uppercase tracking-wider mb-1">
                    @Html.DisplayNameFor(model => model.Fornecedor)
                </p>
                @if (Model?.Fornecedor != null)
                {
                    <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium border border-gray-200 dark:border-gray-700 text-gray-600 dark:text-gray-400">
                        @Model.Fornecedor.Nome
                    </span>
                }
                else
                {
                    <span class="text-sm text-gray-400 dark:text-gray-600">Não cadastrado</span>
                }
            </div>

        </div>

         <!-- Formulário de exclusão -->
         <form id="delete-form" asp-action="Delete" class="space-y-5">
             <input type="hidden" asp-for="Id" />

             <div class="pt-2">
                 <button type="button" id="open-modal-btn"
                         class="w-full py-2 px-4 border border-red-600 dark:border-red-500 text-sm font-medium text-red-600 dark:text-red-500 bg-transparent hover:bg-red-600 hover:text-white dark:hover:bg-red-500 rounded transition-colors duration-200">
                     Confirmar Exclusão
                 </button>
             </div>
         </form>

        <!-- Rodapé com link Cancelar -->
        <div class="mt-6 border-t border-gray-100 dark:border-gray-900 pt-4 text-center">
            <a asp-action="Index"
               class="text-xs text-gray-500 dark:text-gray-400 hover:text-black dark:hover:text-white transition-colors duration-150">
                Cancelar
            </a>
        </div>

     </div>
 </div>

 <!-- Modal de confirmação de exclusão -->
 <div id="deleteConfirmationModal" class="fixed inset-0 z-50 hidden flex items-center justify-center">
     <div class="fixed inset-0 bg-black/50 dark:bg-black/70"></div>
     <div class="relative w-full max-w-sm mx-4 bg-white dark:bg-gray-950 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 p-6">
         <h3 class="text-lg font-medium text-black dark:text-white mb-3">Confirmar exclusão</h3>
         <p class="text-sm text-gray-600 dark:text-gray-400 mb-6">
             Tem certeza que deseja excluir o produto <strong>@Model.Nome</strong>? Esta ação não pode ser desfeita.
         </p>
         <div class="flex gap-3">
             <button type="button" id="cancelDeleteBtn"
                     class="flex-1 py-2 px-4 border border-gray-300 dark:border-gray-700 text-sm font-medium text-gray-900 dark:text-white bg-white dark:bg-gray-900 hover:bg-gray-50 dark:hover:bg-gray-800 rounded transition-colors duration-200">
                 Cancelar
             </button>
             <button type="button" id="confirmDeleteBtn"
                     class="flex-1 py-2 px-4 border border-red-600 dark:border-red-500 text-sm font-medium text-white bg-red-600 dark:bg-red-500 hover:bg-red-700 dark:hover:bg-red-600 rounded transition-colors duration-200">
                 Excluir
             </button>
         </div>
     </div>
 </div>

 @section Scripts {
     <script>
         const modal = document.getElementById('deleteConfirmationModal');
         document.getElementById('open-modal-btn').addEventListener('click', () => modal.classList.remove('hidden'));
         document.getElementById('cancelDeleteBtn').addEventListener('click', () => modal.classList.add('hidden'));
         document.getElementById('confirmDeleteBtn').addEventListener('click', () => { document.getElementById('delete-form').submit(); });
         modal.addEventListener('click', e => { if (e.target === modal) modal.classList.add('hidden'); });
     </script>
 }
